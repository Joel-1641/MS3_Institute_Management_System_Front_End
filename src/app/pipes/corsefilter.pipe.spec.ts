import { CorsefilterPipe } from './corsefilter.pipe';

describe('CorsefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CorsefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
