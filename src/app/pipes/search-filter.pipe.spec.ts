import { SearchCoursePipe } from './search-filter.pipe';

describe('SearchCoursePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCoursePipe();
    expect(pipe).toBeTruthy();
  });
});
